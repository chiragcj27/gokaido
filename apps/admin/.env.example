# Public base URL of the Gokaido API (Express backend).
# Inlined into the client bundle at build time, so set it BEFORE `next build`
# and rebuild if it changes. Must be reachable from the browser and be
# listed in the API's CORS_ORIGINS. Defaults to http://localhost:3001.
NEXT_PUBLIC_API_URL=https://api.example.com
